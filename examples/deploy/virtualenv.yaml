apiVersion: env.alibaba.com/v1alpha1
kind: VirtualEnvironment
metadata:
  name: demo-virtualenv
spec:
  envHeader:
    name: ali-env-mark
    autoInject: true
  envLabel:
    name: virtual-env
    splitter: "-"
    defaultSubset: dev
  instancePostfix: demo
